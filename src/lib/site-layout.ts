/** Contenedor alineado con la portada (hero, cabeceras y cuerpo de página). */
export const siteContainer = 'container mx-auto px-6 md:px-8';

/** Padding vertical de bloques de contenido claros (misma escala que secciones de la home). */
export const siteContentSection = 'py-20 md:py-28';

/** Cabecera franja de marca (páginas interiores). */
export const sitePageHeader = 'py-14 md:py-20';

/** Contenido principal en /admin (SidebarInset): mismos márgenes horizontales que el sitio público. */
export const siteAdminY = 'py-6 md:py-10';
